import java.util.*;

public class cf_05HashSet {
    public static void main(String[] args) {
        HashSet hs = new HashSet<>();
        hs.add​(10);
        System.out.println(hs.isEmpty());// Adds the specified element to this set if it is not already present.
        System.out.println(hs);
        // all method are inherite from collection

    };
}
